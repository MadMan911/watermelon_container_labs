# 🐳 Dockerfile Lab — FastAPI + Best Practices

## 📌 Содержание
- app/ — FastAPI-приложение (Hello, ITMO!)
- bad/Dockerfile — пример плохого Dockerfile
- good/Dockerfile — улучшенный Dockerfile с best practices
- requirements.txt — зависимости для приложения

---

## ❌ Плохие практики в bad/Dockerfile

Проблема -> Почему плохо -> Как исправлено
1) Большой образ (python:3.10) -> Большой размер, лишние зависимости -> Использован python:3.10-slim
2) Установка ненужных пакетов (vim, nano, git) -> Увеличение слоя, лишние зависимости -> Только нужный curl
3) COPY . /app -> В контейнер может попасть лишнее -> Копируется только ./app
4) pip install без флагов -> Занимает больше памяти -> Добавлен --no-cache-dir, pip upgrade
5) Нет переменных среды -> Могут быть лишние байт-коды, буферизация -> Установлены ENV переменные

---

##  bad-practices использования контейнеров

1. Запуск контейнера от root без USER — небезопасно.
2. Хранение данных внутри контейнера без volume — при пересборке всё теряется.

---

## Когда не стоит использовать контейнеры

1. Когда приложение требует нативного GUI или драйверов.
2. Когда время отклика критично (реалтайм системы на edge-устройствах).

---

## Как собрать и запустить

```bash
# Сборка хорошего образа
docker build -t fastapi-itmo -f good/Dockerfile .

# Запуск
docker run -d -p 8000:8000 -v $(pwd)/data:/app/data fastapi-itmo
