FROM python:3.10

RUN apt update && apt install -y python3-pip vim curl git nano

COPY . /app

WORKDIR /app/app

RUN pip install fastapi uvicorn

VOLUME ["/app/data"]

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
